@extends('elearning::layout')

{{-- Hero renders automatically via the layout when routeIs('elearning.index').
     Nothing else goes here — this route's whole job is to be the hero landing page. --}}